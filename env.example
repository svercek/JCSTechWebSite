# Database Configuration
# For local development without database, set SKIP_DB=true


# For production with Neon PostgreSQL database:
# Remove or comment out SKIP_DB above and add your Neon connection string:
# DATABASE_URL=postgresql://username:password@ep-cool-name-123456.us-east-2.aws.neon.tech/jcstech?sslmode=require

# Admin Authentication
# Set a strong password for admin access to /admin/blog
# For production, use a hashed password (bcrypt)
ADMIN_PASSWORD=ThIsThEaDmInPaSsWoRdFoR1601

# Email Configuration (if using nodemailer)
# SMTP_HOST=localhost
# SMTP_PORT=25
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@jcstechnologies.com
